#!/bin/bash
echo "hello, just install some instruments"
#git init
#git config --global user.email "megaton2011@gmail.com”
#git config --global user.name "Dmitry Potapov"
#git clone git@github.com:megaton20111/belhard-devops-1.git
echo "git repo was cloned in locally folder"

#curl -fsSL https://apt.releases.hashicorp.com/gpg | sudo apt-key add -

#sudo apt-add-repository "deb [arch=amd64] https://apt.releases.hashicorp.com $(lsb_release -cs) main"

#sudo apt-get update && sudo apt-get install terraform
#mkdir Terraform
#cd Terraform
#RiseupVPN &
#wget https://releases.hashicorp.com/terraform/1.1.9/terraform_1.1.9_linux_amd64.zip
#unzip terraform_1.1.9_linux_amd64.zip
#rm terraform_1.1.9_linux_amd64.zip
#sudo mv terraform /bin/
#terraform --version
echo "terraform was installed"
echo "inslall atom editor on lokal machine"
#add our official package repository to your system by running the following commands:
#wget -qO - https://packagecloud.io/AtomEditor/atom/gpgkey | sudo apt-key add -
#sudo sh -c 'echo "deb [arch=amd64] https://packagecloud.io/AtomEditor/atom/any/ any main" > /etc/apt/sources.list.d/atom.list'
#install Atom using apt-get (or apt on Ubuntu):
#sudo apt-get update
# Install Atom
#sudo apt-get install atom
# Install Atom Beta
#sudo apt-get install atom-beta
#download the Atom .deb package and install it directly:
# Install Atom
#sudo dpkg -i atom-amd64.deb
# Install Atom's dependencies if they are missing
#sudo apt-get -f install
