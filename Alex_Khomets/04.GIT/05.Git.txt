	1.Создаем папку с 2-мя файлами, инициализируем git
mkdir devops
cd devops
git init
git status
nano text
nano homework
git add text homework
git commit -m  "add text homework"
	2. Создаем ветку staging с файлами из master+ 2 файла
git checkout -b staging
nano staging1
nano staging2
git add --all
git commit -m "add staging1 staging2"
git checkout master
	3. Создаем ветку test с файлами из мастер и staging + 1 файл
git checkout -b test
git rebase staging
nano test
git add test
git commit -m "test"
git checkout master
	4. Создаем ветку fix/prod_is_donw с файлами из master + 1 файл
git checkout -b fix/prod_is_down
nano fix
git add fix
git comment -m "add fix"
git checkout master
	5.Релиз в production - добавляем в ветку master все коммиты в master (кроме коммитов в ветке fix/prod_is_down)
git rebase staging
get rebase test
	6. Добавляем во все ветки коммит из ветки fix/prod_is_down
git rebase fix/prod_is_down
git checkout test
git rebase fix/prod_is_down
git checkout staging
git rebase fix/prod_is_down






